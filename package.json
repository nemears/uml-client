{
  "name": "uml-js",
  "version": "1.0.0",
  "description": "A library for interacting with a uml-server",
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha test/**/*.js --recursive --timeout 60000 --exit"
  },
  "author": "Nicholas Emerson Mears",
  "license": "MIT",
  "dependencies": {
    "mocha": "^10.2.0"
  }
}
